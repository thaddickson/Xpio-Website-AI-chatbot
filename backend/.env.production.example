# Production Environment Configuration for Xpio Health Chatbot

# ============================================================================
# ANTHROPIC API
# ============================================================================
# Get your API key from: https://console.anthropic.com
# WARNING: Keep this secret! Never commit to git or expose to frontend
ANTHROPIC_API_KEY=sk-ant-api03-your-actual-api-key-here

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
PORT=3000
NODE_ENV=production

# Allowed origins for CORS (comma-separated, no spaces)
# Add all domains that will embed the chatbot
ALLOWED_ORIGINS=https://xpiohealth.com,https://www.xpiohealth.com

# ============================================================================
# DATABASE (PostgreSQL)
# ============================================================================
# Format: postgresql://username:password@host:port/database
#
# Railway: Automatically provided - no need to set
# Supabase: Get from Settings > Database > Connection String
# AWS RDS: postgres://username:password@endpoint:5432/dbname
# Local: postgresql://localhost:5432/lead_gen_db

DATABASE_URL=postgresql://username:password@host:port/database_name

# ============================================================================
# EMAIL NOTIFICATIONS
# ============================================================================

# Email Service Provider
# Options: 'gmail', 'sendgrid', 'mailgun', 'smtp'
EMAIL_SERVICE=gmail

# Gmail Setup (Recommended for testing):
# 1. Enable 2FA on your Google account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character app password below (no spaces)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-16-char-app-password

# SendGrid Setup (Recommended for production):
# 1. Sign up at sendgrid.com
# 2. Create API key
# 3. Use 'apikey' as EMAIL_USER and your API key as EMAIL_PASSWORD
# EMAIL_SERVICE=sendgrid
# EMAIL_USER=apikey
# EMAIL_PASSWORD=SG.your-api-key

# Email address to receive lead notifications
NOTIFICATION_EMAIL=notifications@xpiohealth.com

# ============================================================================
# OPTIONAL: CRM INTEGRATION
# ============================================================================
# HubSpot
# HUBSPOT_API_KEY=

# Salesforce
# SALESFORCE_API_KEY=
# SALESFORCE_INSTANCE_URL=

# ============================================================================
# SECURITY
# ============================================================================
# Generate a random string for session encryption
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-random-64-character-hex-string-here

# ============================================================================
# MONITORING (Optional)
# ============================================================================
# Sentry for error tracking
# SENTRY_DSN=

# New Relic for performance monitoring
# NEW_RELIC_LICENSE_KEY=
